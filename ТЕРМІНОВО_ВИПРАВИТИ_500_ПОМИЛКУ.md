# ⚠️ ТЕРМІНОВО: Виправлення помилки HTTP 500

## Проблема
Таблиця `settings` не існує в базі даних, тому сайт видає помилку 500.

## ✅ ШВИДКЕ РІШЕННЯ (2 хвилини)

### Крок 1: Відкрийте phpMyAdmin
1. Зайдіть в ISPManager
2. Знайдіть "Бази даних" → "phpMyAdmin"
3. Виберіть базу даних `tes`

### Крок 2: Виконайте SQL
1. Натисніть на вкладку **"SQL"** вгорі
2. Відкрийте файл `database/fix_all_tables.sql` на вашому комп'ютері
3. Скопіюйте **ВЕСЬ** вміст файлу
4. Вставте в поле SQL редактора
5. Натисніть кнопку **"Вперед"** (або "Execute")

### Крок 3: Перевірте
1. Після виконання SQL оновіть сторінку https://radio-active.top
2. Сайт має запрацювати!
3. Зайдіть в адмін-панель: https://radio-active.top/admin
4. Логін: `admin`
5. Пароль: `admin123` (змініть після входу!)

## Що було зроблено?

SQL скрипт створить 4 таблиці (якщо їх немає):
- ✅ `admins` - адміністратори
- ✅ `contacts` - форма зворотного зв'язку
- ✅ `pages` - додаткові сторінки (Про нас)
- ✅ `settings` - **ВСІ налаштування контенту** (37 параметрів!)

## Після виправлення

Зайдіть в **Адмін-панель → Налаштування** і побачите:

🎯 **Hero секція** - заголовок, опис, кнопка
🏷️ **Логотип** - підзаголовок
📋 **Секція послуг** - заголовки
⭐ **Секція відгуків** - заголовки
✨ **4 переваги** - іконки, заголовки, описи
📄 **Футер** - всі тексти
🏢 **Основні** - телефон, email, адреса

Тепер ВСЕ редагується через адмін-панель!

## Якщо виникли проблеми

1. Переконайтеся що скопіювали ВЕСЬ SQL код
2. Перевірте що вибрана база даних `tes`
3. Подивіться на помилки в phpMyAdmin (якщо є)
4. Переконайтеся що .env файл містить правильні дані:
   ```
   DB_NAME=tes
   DB_USER=tes
   DB_PASS=0607Dm$157
   ```
