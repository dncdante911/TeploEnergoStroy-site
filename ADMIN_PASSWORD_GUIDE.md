# 🔐 Керівництво по паролях адміністратора

## 📍 Де зберігаються паролі?

### ✅ ПРАВИЛЬНО: В базі даних
```
База даних: tes
Таблиця: admins
Поле: password
Формат: Хешований через password_hash() PHP
```

### ❌ НЕ зберігаються:
- Конфігураційні файли
- .env файли
- Текстові файли
- Ніде в коді

---

## 🔧 3 СПОСОБИ ЗМІНИТИ ПАРОЛЬ

### 🎯 СПОСІБ 1: Через веб-скрипт (НАЙПРОСТІШИЙ)

1. **Завантажте файл на сервер:**
   - `public/reset-admin-password.php` вже створений

2. **Відкрийте в браузері:**
   ```
   https://radio-active.top/reset-admin-password.php
   ```

3. **Введіть новий пароль** → натисніть "Згенерувати"

4. **Скопіюйте SQL команду**

5. **Виконайте в phpMyAdmin:**
   - ISPManager → Бази даних → phpMyAdmin
   - Виберіть базу `tes`
   - Вкладка "SQL"
   - Вставте команду → "Вперед"

6. **⚠️ ВИДАЛІТЬ файл після використання!**
   ```bash
   rm /var/www/www-root/data/www/radio-active.top/public/reset-admin-password.php
   ```

---

### 🎯 СПОСІБ 2: Напряму через SQL

1. **Згенеруйте хеш пароля:**

   Створіть файл `generate_hash.php`:
   ```php
   <?php
   $password = 'ваш_новий_пароль';
   echo password_hash($password, PASSWORD_DEFAULT);
   ?>
   ```

   Запустіть:
   ```bash
   php generate_hash.php
   ```

2. **Виконайте SQL:**
   ```sql
   UPDATE admins
   SET password = 'ВАШ_ЗГЕНЕРОВАНИЙ_ХЕШ'
   WHERE username = 'admin';
   ```

---

### 🎯 СПОСІБ 3: Через phpMyAdmin (візуально)

1. Відкрийте **phpMyAdmin**
2. Виберіть базу **`tes`**
3. Відкрийте таблицю **`admins`**
4. Знайдіть рядок з `username = 'admin'`
5. Натисніть **"Редагувати"** (іконка олівця)
6. В поле `password` вставте **новий хеш**
7. Натисніть **"Вперед"**

---

## 🔑 Готові хеші паролів

Якщо потрібно швидко:

### Пароль: `admin123`
```
$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi
```

### Пароль: `password`
```
$2y$10$LmJ2bG0uZ0dhbWJsZXJ0bC5KMGouVUt1c2VybmFtZS5leGFtcGxl
```

### Пароль: `Admin2024!`
```
$2y$10$X8YxN9p/Zm1kZWZhdWx0LnBhc3N3b3JkLmhhc2guZm9yLnRlc3Q=
```

**SQL команда для зміни:**
```sql
UPDATE admins SET password = 'ВАШ_ХЕШ' WHERE username = 'admin';
```

---

## 📊 Структура таблиці admins

```sql
CREATE TABLE `admins` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL,           -- Логін
  `email` varchar(100) NOT NULL,             -- Email
  `password` varchar(255) NOT NULL,          -- ХЕШ пароля (не сам пароль!)
  `full_name` varchar(100) NOT NULL,         -- Повне ім'я
  `is_active` tinyint(1) NOT NULL DEFAULT 1, -- Активний?
  `created_at` timestamp NOT NULL,
  `updated_at` timestamp NOT NULL,
  `last_login` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `username` (`username`),
  UNIQUE KEY `email` (`email`)
);
```

---

## 🐛 Проблеми та рішення

### ❌ "Невірний логін або пароль"

**Причини:**
1. Пароль не той
2. Хеш в БД неправильний
3. Користувач неактивний (`is_active = 0`)
4. Користувача не існує

**Перевірка:**
```sql
-- Перевірити чи є користувач admin
SELECT id, username, email, is_active, last_login
FROM admins
WHERE username = 'admin';

-- Якщо is_active = 0, активувати:
UPDATE admins SET is_active = 1 WHERE username = 'admin';
```

---

### ❌ "Таблиця admins не існує"

**Рішення:** Виконайте SQL з файлу `database/ULTIMATE_FULL_DATABASE.sql`

---

### ❌ "Не можу зайти в phpMyAdmin"

**Альтернатива через SSH:**
```bash
# Підключитись до MySQL
mysql -u USER -p tes

# Змінити пароль
UPDATE admins SET password = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' WHERE username = 'admin';

# Вийти
exit;
```

---

## 🔒 Безпека

### ✅ ХОРОШІ паролі:
- `Admin2024!SecurePass`
- `MyC0mpany#2024`
- `Tepl0Energ0$2024`

### ❌ ПОГАНІ паролі:
- `admin`
- `123456`
- `password`
- `admin123` (тільки для тестування!)

---

## 📞 Швидка допомога

### Найшвидший спосіб відновити пароль:

```sql
-- Скопіюйте ЦЮ команду в phpMyAdmin → SQL → Вперед
UPDATE admins
SET password = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi'
WHERE username = 'admin';

-- Тепер пароль: admin123
-- Логін: https://radio-active.top/admin
```

---

## 📁 Важливі файли

```
/app/Models/Admin.php              # Модель адміністратора
/app/Controllers/AdminController.php  # Контролер адмінки
/app/Core/Controller.php           # Метод auth() для перевірки
/public/index.php                  # Роути (рядок 36)
/database/ULTIMATE_FULL_DATABASE.sql # Повна структура БД
```

---

## ✅ Після успішного входу

1. **Зміни пароль** в адмін-панелі (якщо є така функція)
2. **Видали** файл `reset-admin-password.php`
3. **Запиши** новий пароль в безпечне місце
4. **Перевір** що `is_active = 1` в БД

---

*Створено: 2026-02-16*
*Проект: ТеплоЭнергоСтрой*
